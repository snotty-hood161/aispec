# 前端 PR 评审清单模板

## 文档目标
1. 用于前端 PR 评审，评审人逐项核对，确保代码质量达标。
2. 默认适用 `common` 全量规则，评审前先标注应用类型。

## 使用方式
1. **谁用**：PR 评审人（Reviewer）。
2. **何时用**：每次前端 PR 提交评审时。
3. **怎么用**：复制本清单到 PR 评审评论中，逐项勾选，未通过项写明阻塞原因。

## 前提
1. 评审前先标注应用类型：`admin-console` / `wechat-h5` / `miniprogram`。
2. 每项必须给出结果：`[x]` 通过 / `[ ]` 不通过（需写阻塞原因）。
3. 如有例外，必须在 PR 说明中记录：原因、边界、回收时间。

## 优先级说明
1. `P0` 为阻塞项，必须全部通过才可合并。
2. `P1` 为改进项，允许带条件合并，但必须登记技术债与回收计划。
3. 评审结论遵循：任一 `P0` 未通过则 `Request Changes`。

---

## PR 基本信息
- [ ] [P0] 已标注适用应用类型：`admin-console` / `wechat-h5` / `miniprogram`
- [ ] [P0] 已说明变更目的、影响范围、回滚方案
- [ ] [P0] 已附关键测试结果或截图

## TypeScript 与编码
- [ ] [P0] `strict` 模式开启，无新增无边界 `any`
- [ ] [P0] 导出的函数、组件、Hook/Composable 有显式类型声明
- [ ] [P0] 代码中有中文注释，关键函数说明"做什么"和"为什么"
- [ ] [P0] 无 `console.log/debug/info/warn` 残留（`console.error` 除外）
- [ ] [P0] 无调试代码残留（`debugger`、临时注释块）
- [ ] [P1] 单文件未超过 300 行，超过已拆分

## 组件与结构
- [ ] [P0] 组件分层正确：基础组件无业务请求、业务组件不被跨页面复用、页面组件只做编排
- [ ] [P0] 组件输入通过 props，输出通过事件/回调，无隐式依赖全局变量
- [ ] [P0] 公共组件有空态/加载态/异常态处理
- [ ] [P1] 新增公共组件至少在两个页面或模块复用

## 表单（涉及表单变更时）
- [ ] [P0] 3 个以上字段的表单通过 Schema 驱动渲染，非逐个手写
- [ ] [P0] 校验规则声明在 Schema 中，未在提交回调临时拼装
- [ ] [P0] Schema 为纯数据结构，未内联组件或 JSX/模板

## 样式与适配
- [ ] [P0] 样式使用 Token 值，无硬编码主题色/间距/字号
- [ ] [P0] 后台管理使用 Tailwind CSS，uni-app 使用 UnoCSS
- [ ] [P0] 小程序端无 SVG 资源
- [ ] [P1] 小程序主包体积未超过 2MB

## API 与状态
- [ ] [P0] 接口调用统一通过 services 层，组件内无直接请求
- [ ] [P0] 错误码统一映射，无魔法字符串
- [ ] [P0] 全局状态仅存放跨页面共享数据，页面私有状态未提升到全局
- [ ] [P0] 副作用（事件/定时器/订阅）在组件卸载时清理

## 性能
- [ ] [P0] 大列表（100+ 项）使用虚拟滚动或分页
- [ ] [P0] 高频事件（scroll/resize/input）已做防抖或节流
- [ ] [P0] 路由级组件使用懒加载
- [ ] [P1] 新增依赖 gzip 后未超过 50KB，或 PR 中已说明必要性

## 安全
- [ ] [P0] 无硬编码密钥、token、私有地址
- [ ] [P0] 日志和埋点中无明文敏感信息（手机号、证件号等）

## 质量门禁
- [ ] [P0] `lint` 通过
- [ ] [P0] `typecheck` 通过
- [ ] [P0] `test` 通过
- [ ] [P0] 构建配置已开启 console 移除和注释清理
- [ ] [P1] 公共组件补单测（渲染、props、事件、插槽）
- [ ] [P1] 关键页面附 Lighthouse 跑分截图

## 前后端协作（涉及接口变更时）
- [ ] [P0] 接口契约已与服务端确认
- [ ] [P0] 枚举值使用服务端定义的单一来源
- [ ] [P0] 联调覆盖：成功路径、鉴权失败、参数错误、幂等重试
- [ ] [P1] 发布顺序确认：先服务端兼容发布，再前端切换

---

## 结论
- [ ] `Approve`（全部 `P0` 通过）
- [ ] `Request Changes`（存在任一 `P0` 未通过）
- [ ] `Conditional Approve`（`P0` 通过，存在 `P1` 未通过且已登记技术债）
