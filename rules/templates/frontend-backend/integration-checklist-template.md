# 联调检查清单模板（前后端协作）

## 1. 联调前（必须）
- [ ] 契约文档已评审通过（路径：`填写契约文档所在仓库路径或文档链接`）。
- [ ] 错误码映射已确认（服务端语义 + 前端提示）。
- [ ] 鉴权方式、时区、枚举语义已确认。
- [ ] 环境信息已统一（域名、版本、测试账号、开关配置）。
- [ ] 变更类型已标注（兼容 / 非兼容）。

## 2. 用例覆盖（必须）
- [ ] 成功路径覆盖完成。
- [ ] 参数错误路径覆盖完成。
- [ ] 鉴权失败路径覆盖完成。
- [ ] 幂等重试路径覆盖完成（如适用）。
- [ ] 限流/超时/网络异常路径覆盖完成（如适用）。

## 3. 数据与表现一致性（必须）
- [ ] 枚举值展示与服务端定义一致。
- [ ] 时间字段显示符合约定时区与格式。
- [ ] 空态/异常态文案符合错误码映射规则。
- [ ] 前端未自创服务端未定义语义。

## 4. 质量门禁（必须）
- [ ] 前端 `lint + typecheck + test` 全通过。
- [ ] 服务端对应质量门禁全通过。
- [ ] 关键接口日志包含 traceId/requestId（建议）。

## 5. 联调结论
1. 联调结果：通过 / 不通过
2. 阻塞问题列表（P0）：
3. 非阻塞问题列表（P1）：
4. 责任人及修复截止时间：
5. 复验时间：
