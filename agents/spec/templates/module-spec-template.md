# {模块名称} — 模块级技术规格说明书

> 所属项目：{项目名称}
> 版本：{版本号}
> 日期：{日期}
> 状态：{草案 / 评审中 / 已定稿}

---

## 1. 模块职责与边界

### 1.1 核心职责
{一句话定义模块的核心职责}

### 1.2 负责
1. {职责 1}
2. {职责 2}
3. {职责 3}

### 1.3 不负责
1. {不负责事项 1}（属于 {其他模块}）
2. {不负责事项 2}（属于 {其他模块}）

### 1.4 包含的功能
| 功能 | 优先级 | 描述 |
|------|--------|------|
| {功能名} | P0/P1/P2 | {描述} |

---

## 2. 数据结构

### 2.1 实体（Entity）

#### 实体：{实体名}
| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | bigint | PK, 自增 | 主键 |
| {字段名} | {类型} | {约束} | {说明} |
| created_at | datetime | NOT NULL | 创建时间 |
| updated_at | datetime | NOT NULL | 更新时间 |
| deleted_at | datetime | NULL | 软删除时间 |

**索引：**
- `idx_{field}({field})` — {用途}
- `uk_{field}({field})` — {唯一约束用途}
- `idx_{field1}_{field2}({field1}, {field2})` — {组合查询用途}

### 2.2 值对象（Value Object）

#### {值对象名}
| 字段 | 类型 | 说明 |
|------|------|------|
| {字段名} | {类型} | {说明} |

### 2.3 DTO（Data Transfer Object）

#### {DTO 名} — {用途}
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| {字段名} | {类型} | 是/否 | {说明} |

### 2.4 实体关系
| 实体 A | 关系 | 实体 B | 关联方式 |
|--------|------|--------|---------|
| {实体} | 1:N | {实体} | {外键字段} |

### 2.5 枚举定义
| 枚举名 | 值 | 编码 | 说明 |
|--------|-----|------|------|
| {枚举名}.{项} | {值} | {编码} | {说明} |

---

## 3. API 接口

### 3.1 接口总览

| 编号 | 接口名称 | 方法 | 路由 | 认证 | 说明 |
|------|---------|------|------|------|------|
| API-001 | {名称} | GET/POST/PUT/DELETE | {path} | 是/否 | {说明} |

### 3.2 接口详情

#### API-001：{接口名称}
- **路由**：`{METHOD} {path}`
- **认证**：{需要/不需要}
- **幂等**：{是/否}，幂等键：{字段}
- **限流**：{限流规则}
- **描述**：{接口做什么}

**请求参数：**
| 参数 | 位置 | 类型 | 必填 | 校验规则 | 说明 |
|------|------|------|------|---------|------|
| {参数名} | path/query/body/header | {类型} | 是/否 | {规则} | {说明} |

**响应结构（成功）：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "{字段}": "{类型} — {说明}"
  }
}
```

**响应结构（分页，如适用）：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [],
    "total": 0,
    "page": 1,
    "page_size": 20
  }
}
```

**错误码：**
| 错误码 | 说明 | HTTP Status |
|--------|------|------------|
| {错误码} | {说明} | {Status} |

**调用示例：**
```
请求：
{METHOD} {path}
{请求体示例}

响应（成功）：
{响应体示例}

响应（失败）：
{错误响应示例}
```

---

## 4. 业务规则

| 规则编号 | 规则名称 | 规则描述 | 触发条件 | 执行动作 | 优先级 |
|---------|---------|---------|---------|---------|--------|
| BR-001 | {名称} | {描述} | {条件} | {动作} | P0/P1 |

### 详细规则说明

#### BR-001：{规则名称}
- **描述**：{详细描述}
- **触发条件**：{何时触发}
- **执行逻辑**：
  1. {步骤 1}
  2. {步骤 2}
- **异常处理**：{规则执行失败时的处理}
- **关联规则**：{与其他规则的关系}

---

## 5. 边界条件

| 场景 | 输入 | 期望行为 | 处理方案 | 测试要求 |
|------|------|---------|---------|---------|
| {场景描述} | {输入描述} | {期望行为} | {方案} | 必测/选测 |

### 详细边界场景

#### 极值边界
| 字段/参数 | 最小值 | 最大值 | 超出行为 |
|----------|--------|--------|---------|
| {字段名} | {最小值} | {最大值} | {返回错误/截断/忽略} |

#### 并发边界
| 场景 | 并发操作 | 一致性要求 | 处理方案 |
|------|---------|-----------|---------|
| {场景} | {操作} | {要求} | {乐观锁/悲观锁/队列} |

#### 幂等边界
| 接口 | 幂等键 | 重复行为 | 过期策略 |
|------|--------|---------|---------|
| {接口} | {键} | {返回上次结果/忽略} | {过期时间} |

---

## 6. 状态机

### 6.1 {实体名} 状态机

**状态定义：**
| 状态 | 编码 | 说明 | 是否终态 |
|------|------|------|---------|
| {状态名} | {编码} | {说明} | 是/否 |

**状态转换：**
| 当前状态 | 目标状态 | 触发条件 | 触发角色 | 可逆 | 副作用 |
|---------|---------|---------|---------|------|--------|
| {当前} | {目标} | {条件} | {角色} | 是/否 | {副作用描述} |

**状态流转图（文字描述）：**
```
{初始状态} --[触发条件]--> {中间状态} --[触发条件]--> {终态}
                                    --[触发条件]--> {另一终态}
```

---

## 7. 权限模型

### 7.1 角色权限矩阵

| 操作 | {角色 1} | {角色 2} | {角色 3} | {角色 4} |
|------|---------|---------|---------|---------|
| {操作 1} | ✅ | ✅ | ❌ | ❌ |
| {操作 2} | ✅ | ✅ | 仅自己的 | ❌ |

### 7.2 数据权限

| 角色 | 数据范围 | 说明 |
|------|---------|------|
| {角色} | {全部/本部门/仅自己} | {补充说明} |

---

## 8. 事件与消息

### 8.1 发布的事件

| 事件名 | 触发时机 | Payload | 消费方 | 可靠性要求 |
|--------|---------|---------|--------|-----------|
| {事件名} | {时机} | `{payload 结构}` | {消费方} | {At-least-once/Exactly-once} |

### 8.2 消费的事件

| 事件名 | 来源模块 | 处理逻辑 | 幂等策略 | 失败处理 |
|--------|---------|---------|---------|---------|
| {事件名} | {来源} | {逻辑} | {策略} | {重试/死信队列} |

---

## 9. 错误处理

### 9.1 错误码体系

错误码格式：`{模块前缀}{错误分类}{序号}`

| 错误码 | 含义 | HTTP Status | 用户提示 | 可重试 |
|--------|------|------------|---------|--------|
| {错误码} | {含义} | {Status} | {提示} | 是/否 |

### 9.2 降级策略

| 依赖服务 | 降级方案 | 降级影响 | 恢复策略 |
|---------|---------|---------|---------|
| {服务} | {方案} | {影响} | {恢复} |

### 9.3 重试策略

| 场景 | 最大重试次数 | 重试间隔 | 退避策略 |
|------|------------|---------|---------|
| {场景} | {次数} | {间隔} | {固定/指数退避} |

---

## 10. 模块依赖关系

### 10.1 依赖的模块

| 模块 | 调用接口 | 依赖强度 | 降级方案 |
|------|---------|---------|---------|
| {模块名} | {接口} | 强/弱 | {方案} |

### 10.2 被依赖的模块

| 模块 | 调用接口 | 说明 |
|------|---------|------|
| {模块名} | {接口} | {说明} |

### 10.3 依赖方向图
```
{本模块} → {依赖模块 A}
{本模块} → {依赖模块 B}
{依赖模块 C} → {本模块}
```

---

## 11. 测试策略

### 11.1 单元测试

| 测试场景 | 覆盖的业务规则/边界 | 优先级 |
|---------|-------------------|--------|
| {场景} | {规则编号} | 必测/选测 |

### 11.2 集成测试

| 测试场景 | 涉及模块 | 预置条件 | 期望结果 |
|---------|---------|---------|---------|
| {场景} | {模块} | {条件} | {结果} |

### 11.3 性能测试

| 接口 | 目标 QPS | 目标 P95 | 并发数 | 数据量 |
|------|---------|---------|--------|--------|
| {接口} | {QPS} | {时间} | {并发} | {数据量} |

---

## 12. 迁移与兼容性

### 12.1 API 版本策略
{版本管理方式和向后兼容原则}

### 12.2 数据迁移
| 变更 | 迁移方案 | 回滚方案 | 影响范围 |
|------|---------|---------|---------|
| {变更描述} | {方案} | {回滚} | {影响} |

### 12.3 功能开关
| 功能 | 开关名 | 默认值 | 灰度策略 |
|------|--------|--------|---------|
| {功能} | {开关} | {默认} | {策略} |

### 12.4 弃用计划
| 接口/功能 | 弃用版本 | 下线时间 | 替代方案 |
|----------|---------|---------|---------|
| {项目} | {版本} | {时间} | {替代} |
